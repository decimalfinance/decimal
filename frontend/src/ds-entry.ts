// Design-sync bundle entry: the decimal design system's React surface.
// (The larger design system is the CSS class vocabulary — shipped via styles.)
export { PageHead, Pill, SLPill, OriginPill } from './dec/primitives';
export { Ico } from './dec/icons';
// Reference product-screen mocks (mocks.tsx is design-sync-only, not used by the app).
export { BillList, BillReview, FlowCanvas, PaymentTracker } from './dec/mocks';
