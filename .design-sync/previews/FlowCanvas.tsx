import React from 'react';
import { FlowCanvas } from 'decimal-frontend';

// Approval flow-builder canvas: stages, step cards, a condition branch.
export const Screen = () => (
  <div className="dec">
    <FlowCanvas />
  </div>
);
