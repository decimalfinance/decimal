# OS and editors
.DS_Store

# Secrets. Never commit these.
.env
.env.*
!.env.example
!**/.env.example
*.pem
*.key
*.p12
*.pfx
api/.secrets/
# Local test-org credentials
TESTING-LABS.md

# Dependencies and build output
node_modules
api/dist/
frontend/dist/
*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# Logs and dumps
*.jsonl
*.jsonl.gz
db-queries.txt
backups/
tmp/

# Tooling state
.vercel
.playwright-mcp/
.bench/
.claude/settings.local.json
.claude/launch.json
.claude/worktrees/

# Design-sync tool: generated bundles, caches and its own dependencies
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Regenerable test data. Rebuild with scripts/invoice-gen (see TESTING-INVOICES.md).
synthetic_data/

# One-off local scripts. Tracked ones were force-added on purpose.
api/scripts/

# Local-only material: pitch, grants, applications, design handoffs, screenshots.
# Kept out of the repo so it stays technical.
/outputs/
/deliverables/
/applications/
/design_handoff_decimal_landing/
/decimal-screen/
/ramp_billpay/
/*.zip
# Only assets/brand is tracked. Everything else under assets/ is local.
/assets/*
!/assets/brand/
